pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        maven { url = uri("https://maven.hytale.com/releases") }
    }
}

rootProject.name = "cave-horror-white-eyes"
